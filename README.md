# Week1Task19 — MCP-сервер и MCP-клиент (Git + погода + планировщик + пайплайны)

Минимальный учебный проект на Python с использованием официального
[MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk).
Демонстрирует, что MCP-соединение по транспорту **stdio** устанавливается,
клиент корректно получает список инструментов и умеет вызывать их вручную.
Добавлены погодные инструменты (Open-Meteo), команда агента `/ask-weather`
(LLM DeepSeek), фоновый планировщик задач (SQLite, псевдо-24/7) и движок
пайплайнов (Wikipedia → LLM → файл).

## Назначение

- `mcp_server.py` — MCP-сервер с инструментами Git, погоды, планировщика и пайплайнов.
- `client/` + `cli.py` — MCP-клиент с интерактивным режимом (`/help`, `/tools`,
  `/call`, `/weather`, `/ask-weather`, `/schedule`, `/summary`, `/schedules`,
  `/pipeline`, `/status`, `/reconnect`, `/exit`) и одноразовым режимом `--once`.
- `llm/` — провайдер LLM (DeepSeek) для `/ask-weather`, `/summary` и `summarize`.
- `scheduler/` — SQLite-хранилище, фоновый планировщик и агрегация/LLM-summary.
- `pipelines/` — движок пайплайнов (резолвер шаблонов, последовательное выполнение).

Проект — **задел на будущие задания**: архитектура разделена так, чтобы позже
можно было легко добавить новые инструменты, отложенное/периодическое
выполнение и автономный tool use агента. Сейчас эти возможности не реализованы.

## Что такое MCP-сервер и MCP-клиент

- **MCP-сервер** — процесс, который предоставляет «инструменты» (функции) по
  протоколу Model Context Protocol. Здесь это обёртка над локальным `git`,
  погодным API Open-Meteo и планировщиком задач с SQLite-хранилищем.
- **MCP-клиент** — процесс, который подключается к серверу, запрашивает список
  доступных инструментов и вызывает их. Здесь клиент сам запускает сервер как
  подпроцесс и общается с ним по stdio.

## Требования

- Python 3.10+
- Установленный `git`, доступный в `PATH`

## Установка зависимостей

```bash
pip install -r requirements.txt
```

В `requirements.txt` указаны `mcp>=2.0.0,<3.0` и `httpx` (HTTP-запросы к Open-Meteo и DeepSeek).
Для SQLite используется стандартный модуль `sqlite3` (входит в Python, отдельная установка не нужна).

> Важно: используется **mcp 2.x**. В этой версии SDK класс `FastMCP` (из 1.x)
> переименован в `MCPServer`, поэтому сервер написан на актуальном API 2.x.

## Как задать GIT_REPO_PATH

Инструменты работают с репозиторием, путь к которому задаётся через переменную
окружения `GIT_REPO_PATH` (по умолчанию — текущая директория).

Windows (PowerShell):

```powershell
$env:GIT_REPO_PATH = "C:\путь\к\репозиторию"
```

Linux/macOS:

```bash
export GIT_REPO_PATH="/путь/к/репозиторию"
```

Также путь можно передать прямо в вызове `git_log` через параметр `repo_path`.

## Как запустить клиент

```bash
python cli.py
```

Одноразовый режим для быстрой проверки:

```bash
python cli.py --once
```

## Ожидаемый вывод

`python cli.py --once` подключается к серверу, выводит список инструментов
(имя, описание, JSON-схему входных параметров) и результат `git_status`:

```text
Соединение установлено.

Доступно инструментов: 16

1. git_status
   Описание: Возвращает статус Git-репозитория: вывод ``git status --short`` и ``git status -sb``.
   Входные параметры (JSON-схема):
     { "properties": {}, "type": "object", "title": "git_statusArguments" }

2. git_log
   Описание: Возвращает список последних N коммитов репозитория: hash, author, date, message.
   Входные параметры (JSON-схема):
     {
       "properties": {
         "limit": { "default": 10, "description": "Сколько последних коммитов вернуть", "minimum": 1, "type": "integer" },
         "repo_path": { "anyOf": [ { "type": "string" }, { "type": "null" } ], "default": null, "description": "Путь к репозиторию..." }
       },
       "type": "object"
     }

=== Результат вызова git_status ===
Репозиторий: C:\путь\к\репозиторию

=== git status --short ===
(нет изменений)

=== git status -sb ===
## main
```

## Команды CLI

Все команды вводятся с префиксом `/`.

| Команда | Описание |
| --- | --- |
| `/help` | показать список всех команд с описанием |
| `/tools` | повторно запросить у MCP-сервера список инструментов и вывести его |
| `/call <tool_name> [args...]` | вызвать инструмент MCP вручную (например, `/call git_status` или `/call git_log limit=5`) |
| `/weather <city> [--days N] [--units metric\|imperial]` | показать погоду: текущую или прогноз на N дней |
| `/ask-weather <city> [вопрос]` | получить данные о погоде через MCP и передать их в LLM для ответа |
| `/schedule <list\|reminder\|weather\|pipeline\|cancel\|pause\|resume> ...` | управление задачами планировщика |
| `/summary <city>\|--all\|--reminders [--days N]` | сводка по собранной погоде |
| `/schedules events [--since <ISO>]` | список неотвеченных событий (push-уведомлений) |
| `/pipeline <run\|list\|show\|reload> ...` | запуск и управление пайплайнами |
| `/status` | показать состояние MCP-соединения: подключение, имя сервера, версию протокола, инструменты, время последнего вызова |
| `/reconnect` | переустановить MCP-соединение: закрыть сессию, перезапустить сервер, initialize и заново получить инструменты |
| `/exit` (или `/quit`) | выйти из программы с корректным закрытием MCP-соединения |

Список команд формируется из единого источника (словарь `COMMANDS` в `cli.py`),
поэтому добавление новой команды автоматически отражается в `/help`.

### Примеры вызова инструментов

```text
> /call git_status
> /call git_log limit=3
> /call git_log limit=3 repo_path="C:\путь\к\репозиторию"
```

Аргументы задаются в виде `ключ=значение`; значения с пробелами заключаются в
двойные кавычки. Типы аргументов приводятся по JSON-схеме инструмента.

## Инструменты сервера

### `git_status`

Возвращает вывод `git status --short` и `git status -sb` для репозитория из
`GIT_REPO_PATH` (или текущей директории). Параметров нет.

### `git_log`

Возвращает список последних N коммитов: `hash`, `author`, `date`, `message`.

Параметры:

- `limit` (`int`, по умолчанию `10`) — сколько коммитов вернуть;
- `repo_path` (`str`, опционально) — путь к репозиторию (если не задан — берётся `GIT_REPO_PATH`).

### `get_current_weather`

Возвращает текстовое описание текущей погоды для города: температура, ветер,
влажность, осадки и краткое описание.

Параметры:

- `city` (`str`, обязательный) — название города (например, `Moscow`, `London`);
- `units` (`str`, по умолчанию `metric`) — `metric` (Цельсий, км/ч) или `imperial` (Фаренгейт, mph).

Пример через `/call`:

```text
> /call get_current_weather city=Moscow
```

### `get_forecast`

Возвращает текстовый прогноз на N дней: по каждому дню дата, температура
(min/max), осадки, ветер.

Параметры:

- `city` (`str`, обязательный);
- `days` (`int`, по умолчанию `3`, максимум `7`);
- `units` (`str`, по умолчанию `metric`).

Пример через `/call`:

```text
> /call get_forecast city=London days=5
```

### Инструменты планировщика

Планировщик добавляет инструменты для управления отложенными/периодическими
задачами. Описания и параметры — на русском.

#### `schedule_reminder`

Создаёт разовое или периодическое напоминание. Возвращает `id` задачи.

Параметры: `text` (обязательный), `at` (ISO 8601), `in_minutes`, `interval_seconds`.

#### `schedule_weather_collection`

Создаёт периодический сбор погоды для города. Возвращает `id` задачи.

Параметры: `city` (обязательный), `interval_minutes` (обязательный, минимум 1), `units` (по умолчанию `metric`).

#### `list_schedules`

Список всех задач со статусом и временем следующего запуска. Параметр: `status` (фильтр).

#### `cancel_schedule` / `pause_schedule` / `resume_schedule`

Изменяют статус задачи по `id`: `cancelled` / `paused` / `active`.

#### `get_summary`

Агрегированные данные (среднее/min/max температура, осадки, число срабатываний)
+ текстовое summary от LLM. Параметры: `city`, `days` (по умолчанию 1),
`include_reminders`, `include_all_cities`. Если ключ DeepSeek не задан — вернёт
агрегаты с пометкой, что LLM недоступен.

#### `get_due_results`

Результаты и неотвеченные события (push-уведомления). Параметр: `since`.
События помечаются прочитанными.

### Инструменты пайплайнов

#### `search`

Поиск в Wikipedia (без ключа). Параметры: `query` (обязательный), `limit`
(1–5, по умолчанию 1), `language` (по умолчанию `ru`). Возвращает
`{"results": [{"title", "extract", "url"}]}`.

#### `summarize`

Сжатие текста через LLM (DeepSeek). Параметры: `text` (обязательный),
`max_length` (по умолчанию 500), `style`. Требует `DEEPSEEK_API_KEY`.

#### `saveToFile`

Сохранение содержимого в файл. Параметры: `content` (обязательный),
`path` (опционально), `format` (`md`/`txt`/`json`). Если `path` не задан —
создаётся `reports/report_<timestamp>.<format>`. Запись разрешена только
внутри корня проекта.

#### `schedule_pipeline`

Планирование запуска пайплайна. Параметры: `pipeline_name`, `args` (JSON),
`interval_minutes` (периодически) или `in_minutes` (разово).

## Погодные инструменты и используемый API

Погодные инструменты работают через бесплатный API [Open-Meteo](https://open-meteo.com/en/docs)
без API-ключа и регистрации:

- геокодинг (город → координаты): `https://geocoding-api.open-meteo.com/v1/search`;
- прогноз/текущая погода: `https://api.open-meteo.com/v1/forecast`.

Если город не найден, возвращается `Город "<city>" не найден.`; при недоступности
API — `Не удалось получить данные о погоде: <причина>`; при некорректных
параметрах (пустой город, `days > 7`) — `Некорректный параметр: <описание>`.

## Команды `/weather` и `/ask-weather`

### `/weather`

Синтаксис: `/weather <city> [--days N] [--units metric|imperial]`.

- без `--days` — текущая погода (`get_current_weather`);
- с `--days N` — прогноз на N дней (`get_forecast`).

Примеры:

```text
> /weather Moscow
> /weather London --days 5
> /weather Tokyo --units imperial
```

### `/ask-weather`

Синтаксис: `/ask-weather <city> [вопрос]`.

Команда вызывает погодный инструмент через MCP, получает данные и передаёт их
в LLM (DeepSeek) вместе с вопросом пользователя, после чего выводит ответ агента.

- если вопрос указан — промт: «Данные о погоде: `<данные>`. Вопрос пользователя:
  `<вопрос>`. Ответь, используя эти данные.»;
- если вопрос не указан — стандартный промт: «Опиши текущую погоду и дай
  рекомендацию по одежде.».

API-ключ DeepSeek берётся из переменной окружения `DEEPSEEK_API_KEY`; если он
не задан, выводится понятная ошибка.

```text
> /ask-weather Moscow Какая сегодня погода и что надеть?
```

## Пример сценария

Пользователь вводит:

```text
> /ask-weather Moscow Какая сегодня погода?
```

Приложение:

1. вызывает `get_current_weather("Moscow")` через MCP и получает данные;
2. формирует промт из данных о погоде и вопроса пользователя;
3. передаёт промт в LLM (DeepSeek) и выводит ответ агента.

## Планировщик задач (псевдо-24/7)

MCP-сервер продолжает работать через **stdio** (клиент запускает его как
подпроцесс). «24/7» достигается за счёт:

- **SQLite-хранилища** — все задачи и результаты персистентны;
- **фонового планировщика** внутри сервера (asyncio-задача, проверяет БД каждые 20 с);
- **открытого CLI-клиента** — его можно держать запущенным сколь угодно долго.

При перезапуске клиента сервер подхватывает задачи из БД и продолжает работу.

## Хранилище (SQLite)

Один файл `scheduler.db` в корне проекта (в `.gitignore`). Путь настраивается
через переменную окружения `SCHEDULER_DB_PATH` (по умолчанию — `scheduler.db`
рядом с `mcp_server.py`).

Таблицы:

- `schedules` — задачи (`type`: `reminder` | `weather_collection`, `params` JSON,
  `interval_seconds`, `run_at`, `next_run_at`, `last_run_at`, `status`);
- `results` — результаты выполнения (`status`: `success` | `error` | `missed`, `payload` JSON);
- `events` — события/push-уведомления (`type`, `message`, `schedule_id`, `acknowledged`).

## Push-уведомления

При срабатывании задачи сервер отправляет клиенту `notifications/message`.
CLI печатает его в консоль между вводами пользователя с префиксом
`[УВЕДОМЛЕНИЕ]`, одновременно событие сохраняется в таблице `events`.

Пример: `[УВЕДОМЛЕНИЕ] reminder_fired: Пора проверить почту`.

## Поведение при перезапуске

При старте сервера для задач с `next_run_at < now`:

- задача выполняется один раз «сейчас»;
- остальные пропущенные срабатывания (если их было бы несколько) помечаются в
  `results` как `missed` без выполнения.

Разовое напоминание после срабатывания получает `status = 'completed'`;
периодическое напоминание и сбор погоды работают, пока не вызван `cancel_schedule`.

## Новые CLI-команды планировщика

```text
/schedule list [--status active|paused|cancelled|completed]
/schedule reminder "<текст>" [--at <ISO> | --in <N>] [--every <N>]   # --in — минуты, --every — секунды
/schedule weather <city> --every <N>                                  # N — минуты
/schedule cancel <id>
/schedule pause <id>
/schedule resume <id>

/summary <city> [--days N]
/summary --all [--days N]
/summary --reminders [--days N]

/schedules events [--since <ISO>]
```

## Пример сценария (планировщик)

1. Создаём периодический сбор погоды:

   ```text
   > /schedule weather Moscow --every 1
   ```

2. Ждём уведомления (очередной тик планировщика, раз в ~20 с):

   ```text
   [УВЕДОМЛЕНИЕ] collection_done: собрана погода для Moscow
   ```

3. Запрашиваем сводку и смотрим задачи/события:

   ```text
   > /summary Moscow --days 1
   > /schedule list
   > /schedules events
   ```

## Пайплайны

Движок пайплайнов (`pipelines/`) связывает инструменты в цепочки: каждый шаг
вызывает MCP-инструмент, а результат передаётся следующему шагу через шаблоны
`{{...}}`. Шаги выполняются **последовательно**; при ошибке шага пайплайн
прерывается и возвращает имя шага, инструмент, текст ошибки и лог промежуточных
результатов.

## Формат `pipelines.json`

```json
{
  "pipelines": {
    "research": {
      "description": "Поиск в Wikipedia, сжатие и сохранение в файл",
      "steps": [
        { "name": "search", "tool": "search", "input": { "query": "{{args.query}}", "limit": 3 } },
        { "name": "summary", "tool": "summarize", "input": { "text": "{{steps.search.output.results[0].extract}}", "max_length": 400 } },
        { "name": "save", "tool": "saveToFile", "input": { "content": "{{steps.summary.output}}", "format": "md" } }
      ]
    }
  }
}
```

Синтаксис ссылок:

- `{{args.<name>}}` — аргумент запуска пайплайна;
- `{{steps.<step>.output}}` — выход предыдущего шага;
- `{{steps.<step>.output.<путь>}}` — вложенный путь (например, `results[0].extract`).

Если путь не разрешается — пайплайн прерывается с ошибкой
`Не удалось разрешить ссылку: <ссылка>`.

## Новые CLI-команды пайплайнов

```text
/pipeline run <name> [key=value ...] [--debug]
/pipeline list
/pipeline show <name>
/pipeline reload

/schedule pipeline <name> [key=value ...] --every <N>   # периодически (минуты)
/schedule pipeline <name> [key=value ...] --in <N>      # разово (минуты)
```

## Отладочный режим

`/pipeline run <name> --debug` печатает после каждого шага: имя шага, инструмент,
разрешённый вход и краткий выход (тип, размер, первые символы). Без `--debug`
выводится только итоговый результат.

## Пример сценария (пайплайн)

1. Ручной запуск пайплайна `research`:

   ```text
   > /pipeline run research query="Искусственный интеллект"
   ```

2. Планирование раз в сутки:

   ```text
   > /schedule pipeline research query="Искусственный интеллект" --every 1440
   ```

3. Дожидаемся уведомления `[УВЕДОМЛЕНИЕ] pipeline_done: ...`.

4. Смотрим созданный файл в папке `reports/`.

## Безопасность `saveToFile`

`saveToFile` запрещает запись за пределы корня проекта (блокирует `../` и
абсолютные пути вне проекта). При попытке возвращается ошибка.

## Обработка ошибок

Сервер не падает при ошибках: если `git` не установлен, путь не существует,
путь не является репозиторием или команда завершилась с ошибкой — инструмент
возвращает понятное сообщение об ошибке (на русском). Погодные инструменты
аналогично возвращают сообщения при недоступном API, ненайденном городе или
некорректных параметрах.

## Структура проекта

```text
Week1Task19/
├── mcp_server.py          # MCP-сервер: Git, погода, планировщик, пайплайны
├── client/
│   ├── __init__.py
│   ├── session.py         # транспорт/сессия: connect, initialize, list_tools, call_tool + уведомления
│   └── logic.py           # высокоуровневая логика
├── llm/
│   ├── __init__.py
│   └── provider.py        # вызов LLM (DeepSeek) для /ask-weather, /summary, summarize
├── scheduler/
│   ├── __init__.py        # AppContext (БД + сессия для уведомлений)
│   ├── db.py              # SQLite: схема, CRUD, агрегаты
│   ├── scheduler.py       # asyncio-задача, обработка due-задач
│   ├── jobs.py            # выполнение reminder, weather_collection, pipeline
│   └── summary.py         # агрегаты + LLM-summary
├── pipelines/
│   ├── __init__.py
│   ├── engine.py          # загрузка, резолв шаблонов, последовательное выполнение
│   ├── resolver.py        # резолвер {{args.x}}, {{steps.y.output.z}}
│   └── errors.py          # PipelineError и др.
├── cli.py                 # точка входа: интерактивный режим + --once
├── pipelines.json         # определения пайплайнов
├── reports/               # файлы, создаваемые пайплайнами (в .gitignore)
├── config.example.json    # пример конфига (пути, опции) — справочно
├── requirements.txt
├── README.md
└── .gitignore
```

### Разделение слоёв (задел на будущее)

- `client/session.py` — только транспорт и сессия (`MCPSession`): запуск сервера,
  `initialize`, `list_tools`, `call_tool`, приём push-уведомлений.
- `client/logic.py` — высокоуровневая логика (`MCPApp`): решает, что вызывать,
  кэширует инструменты, форматирует результаты, готовит промты для LLM.
- `llm/provider.py` — изолированный вызов LLM (DeepSeek).
- `scheduler/` — хранилище (SQLite), фоновый планировщик, логика задач, summary.
- `pipelines/` — движок пайплайнов: загрузка конфига, резолв шаблонов, выполнение.
- `cli.py` — тонкая обёртка: разбор командной строки и цикл ввода.

## Что дальше (план расширения)

Сейчас реализован каркас с Git-инструментами, погодой, планировщиком и
пайплайнами. Возможные следующие шаги:

1. **Новые инструменты** — достаточно добавить функцию с декоратором
   `@server.tool()` в `mcp_server.py`; клиент подхватит её автоматически.
2. **Автономный tool use агент** — цикл «решение → вызов инструмента → анализ
   результата» поверх `MCPApp.call_tool`, без изменений в `client/session.py`.
3. **Расширение планировщика** — новые типы задач в `scheduler/jobs.py`,
   cron-подобные расписания, повторы по дням недели.
4. **Расширение пайплайнов** — условные шаги, ветвления, передача результата
   пайплайна в планировщик, больше типов шаблонов.
5. **Другие транспорты** — сейчас только stdio (по заданию); `MCPSession`
   изолирует параметры подключения, что упрощает переход на другие транспорты.

